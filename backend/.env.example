# ============================================================================
# SPAARBOT ENVIRONMENT CONFIGURATION
# ============================================================================

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_WEBHOOK_URL=https://yourdomain.com/webhook

# Database Configuration
DATABASE_URL=sqlite+aiosqlite:///./spaarbot.db
# For PostgreSQL: DATABASE_URL=postgresql+asyncpg://user:password@localhost/spaarbot

# API Configuration
API_V1_PREFIX=/api/v1
SECRET_KEY=your_secret_key_here_generate_with_openssl

# PayPal Configuration
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here
PAYPAL_MODE=sandbox  # Use 'sandbox' for testing, 'live' for production
PAYPAL_WEBHOOK_ID=your_paypal_webhook_id_here
PAYPAL_PREMIUM_PLAN_ID=your_paypal_plan_id_here  # Created once during setup

# AI Configuration (Groq)
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# Celery & Redis Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://yourdomain.com

# Environment
ENVIRONMENT=development  # Use 'production' for production
DEBUG=True  # Set to False in production

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================

# 1. PAYPAL SETUP
# ----------------
# a) Go to https://developer.paypal.com/
# b) Create a new app (Sandbox or Live)
# c) Get your Client ID and Secret
# d) Create Product: POST /v1/catalogs/products
# e) Create Billing Plan: POST /v1/billing/plans (€2.99/month)
# f) Save the Plan ID in PAYPAL_PREMIUM_PLAN_ID
# g) Configure Webhook in PayPal Dashboard
#    - Events: BILLING.SUBSCRIPTION.*, PAYMENT.SALE.COMPLETED
#    - URL: https://yourdomain.com/api/v1/paypal/webhook
# h) Save Webhook ID in PAYPAL_WEBHOOK_ID

# 2. GROQ API SETUP
# -----------------
# a) Go to https://console.groq.com/
# b) Create API Key
# c) Copy key to GROQ_API_KEY

# 3. REDIS SETUP
# --------------
# Install Redis:
# - Ubuntu: sudo apt install redis-server
# - Mac: brew install redis
# - Docker: docker run -d -p 6379:6379 redis:alpine
#
# Start Redis:
# - Linux/Mac: redis-server
# - Docker: already running

# 4. CELERY SETUP
# ---------------
# Start Celery Worker:
# celery -A app.core.celery_app worker --loglevel=info
#
# Start Celery Beat (for scheduled tasks):
# celery -A app.core.celery_app beat --loglevel=info
#
# Or start both together:
# celery -A app.core.celery_app worker --beat --loglevel=info

# 5. TESSERACT OCR SETUP (for receipt scanning)
# ----------------------------------------------
# Install Tesseract:
# - Ubuntu: sudo apt install tesseract-ocr tesseract-ocr-deu
# - Mac: brew install tesseract tesseract-lang
# - Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
#
# Install German language pack:
# - Ubuntu: sudo apt install tesseract-ocr-deu
# - Mac: brew install tesseract-lang

# 6. GENERATE SECRET KEY
# ----------------------
# openssl rand -hex 32

# 7. DATABASE MIGRATIONS
# ----------------------
# Initialize Alembic:
# alembic init alembic
#
# Create migration:
# alembic revision --autogenerate -m "Add new tables"
#
# Apply migrations:
# alembic upgrade head
