# SpaarBot - Setup Guide

## ✨ Что реализовано

### 🎯 Полностью рабочие модули:

1. **📊 Статистика (Stats)**
   - Реальные данные из API
   - Интерактивные графики с анимациями
   - Фильтры по периодам (неделя, месяц, 3 месяца, год)
   - Экспорт данных в CSV
   - Полный перевод на 4 языка (DE, EN, RU, UK)

2. **🤖 AI Chat**
   - Подключение к Groq API (Llama-3.3-70b)
   - 6 готовых промптов для быстрого доступа
   - Защита от вредных промптов на frontend
   - Многоязычная поддержка
   - Красивый UI с анимациями

3. **📅 Подписки (Subscriptions)**
   - Добавление новых подписок через модальное окно
   - Интерактивный календарь для просмотра дат платежей
   - Удаление подписок
   - Подсчет месячных и годовых расходов
   - Реальное API подключение

4. **🌐 Интернационализация (i18n)**
   - Полная поддержка 4 языков: Немецкий, Английский, Русский, Украинский
   - Динамическая смена языка
   - Все новые фичи переведены

5. **🔗 API Integration**
   - Централизованный API клиент (`/frontend/src/lib/api.ts`)
   - Автоматическая аутентификация через Telegram
   - Обработка ошибок
   - TypeScript типизация

### 🏗️ Подготовлено для будущей разработки:

- **CSV Import**: Backend готов, нужен только UI
- **Open Banking**: Базовая структура с GoCardless
- **PayPal Integration**: OAuth flow и sync endpoints

---

## 🚀 Запуск приложения

### 1. Backend (FastAPI)

```bash
cd backend

# Установка зависимостей
pip install -r requirements.txt

# Создать .env файл
cp .env.example .env

# Заполнить .env:
# - TELEGRAM_BOT_TOKEN (обязательно)
# - GROQ_API_KEY (для AI функционала)
# - SECRET_KEY (любая случайная строка)
# - DATABASE_URL (по умолчанию SQLite)

# Запустить сервер
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен по адресу: `http://localhost:8000`

API документация: `http://localhost:8000/docs`

### 2. Frontend (React + Vite)

```bash
cd frontend

# Установка зависимостей
npm install

# .env уже создан с правильными настройками
# VITE_API_URL=http://localhost:8000
# VITE_API_PREFIX=/api/v1

# Запустить dev сервер
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:5173`

### 3. Telegram Bot (опционально)

```bash
cd backend

# Запустить бота (в отдельном терминале)
python -m app.bot.main
```

---

## 📦 Структура проекта

```
SpaarBot/
├── frontend/
│   ├── src/
│   │   ├── lib/
│   │   │   └── api.ts           # 🆕 API клиент
│   │   ├── pages/
│   │   │   ├── Stats.tsx        # ✅ Полностью переделан
│   │   │   ├── AIChat.tsx       # ✅ Полностью переделан
│   │   │   ├── Subscriptions.tsx # ✅ Полностью переделан
│   │   │   └── ...
│   │   ├── locales/
│   │   │   ├── de.json          # ✅ Обновлены переводы
│   │   │   ├── en.json          # ✅ Обновлены переводы
│   │   │   ├── ru.json          # ✅ Обновлены переводы
│   │   │   └── uk.json          # ✅ Обновлены переводы
│   │   └── ...
│   ├── .env                     # 🆕 Создан
│   └── package.json
├── backend/
│   ├── app/
│   │   ├── api/v1/endpoints/
│   │   ├── services/
│   │   ├── db/
│   │   └── ...
│   ├── .env                     # Нужно создать
│   └── requirements.txt
└── SETUP.md                     # Этот файл
```

---

## 🔧 Конфигурация

### Backend .env

```env
# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_WEBHOOK_URL=

# Database
DATABASE_URL=sqlite+aiosqlite:///./spaarbot.db

# API
SECRET_KEY=your-secret-key-here

# AI (Groq)
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# PayPal (опционально)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_MODE=sandbox

# Open Banking (опционально)
GOCARDLESS_SECRET_ID=
GOCARDLESS_SECRET_KEY=
GOCARDLESS_MODE=sandbox

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Environment
ENVIRONMENT=development
DEBUG=True
```

---

## 🧪 Тестирование

### 1. Проверить API

```bash
# GET статус сервера
curl http://localhost:8000/

# GET статистики (нужен telegram_id)
curl "http://localhost:8000/api/v1/stats/overview?telegram_id=123456789&period=month"

# POST создание пользователя
curl -X POST http://localhost:8000/api/v1/users \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 123456789, "first_name": "Test", "language_code": "de"}'
```

### 2. Проверить Frontend

1. Откройте http://localhost:5173
2. Должен загрузиться интерфейс приложения
3. Навигация работает
4. Все страницы отображаются

---

## 🎨 Что изменилось

### Критические изменения:

1. **API Client** (`/frontend/src/lib/api.ts`)
   - Централизованное управление всеми API запросами
   - Автоматические заголовки с Telegram данными
   - Обработка ошибок
   - Поддержка всех endpoints

2. **Stats Page** (`/frontend/src/pages/Stats.tsx`)
   - Убраны все mock данные
   - Подключение к реальному API
   - Динамические графики на основе транзакций
   - Рабочий экспорт в CSV
   - Loading states и error handling

3. **AI Chat** (`/frontend/src/pages/AIChat.tsx`)
   - Реальное подключение к Groq API через backend
   - 6 готовых промптов (анализ, советы, прогнозы)
   - Защита от вредных промптов
   - Многоязычность
   - Улучшенный UI

4. **Subscriptions** (`/frontend/src/pages/Subscriptions.tsx`)
   - Модальное окно для добавления подписок
   - Интерактивный календарь
   - Реальное API (create, delete, list)
   - Подсчет годовых расходов

5. **Translations** (все `locales/*.json`)
   - Добавлены переводы для Stats
   - Добавлены переводы для AI (quick prompts, suggestions)
   - Исправлен перевод "Wallet" в навигации
   - Полная поддержка 4 языков

---

## 🔐 Безопасность

### Реализованная защита:

1. **AI Chat**:
   - Frontend проверка на вредные промпты
   - Backend валидация через system prompts
   - Rate limiting (TODO на production)

2. **API**:
   - Telegram authentication через init_data
   - CORS настройки
   - Input validation через Pydantic

### Что добавить на production:

- [ ] Rate limiting (slowapi)
- [ ] Redis caching
- [ ] PostgreSQL вместо SQLite
- [ ] Monitoring (Sentry)
- [ ] HTTPS
- [ ] Environment-based secrets management

---

## 📝 Следующие шаги

### Для запуска в production:

1. **Миграция БД**: SQLite → PostgreSQL
2. **Caching**: Redis для AI ответов и статистики
3. **Rate Limiting**: Защита API от abuse
4. **Monitoring**: Sentry для ошибок
5. **CI/CD**: Автоматические тесты и деплой
6. **CSV Import UI**: Frontend для банковского импорта
7. **Open Banking**: Завершение интеграции с GoCardless
8. **PayPal**: UI для подключения аккаунта

### Дополнительные фичи:

- [ ] Уведомления о подписках (уже есть backend)
- [ ] Budget tracking
- [ ] Multi-currency support
- [ ] Transaction receipts / attachments
- [ ] AI-powered insights dashboard

---

## ❓ FAQ

**Q: Приложение не подключается к API**
A: Проверьте, что backend запущен на порту 8000 и frontend .env имеет правильный VITE_API_URL

**Q: AI не отвечает**
A: Убедитесь, что GROQ_API_KEY установлен в backend/.env

**Q: Нет данных в статистике**
A: Добавьте транзакции через API или Telegram бота

**Q: Ошибка "User not authenticated"**
A: Приложение требует запуска через Telegram Mini App или нужно мокировать telegram_id

---

## 💡 Советы

1. **Для разработки**: Используйте hardcoded telegram_id в userStore.ts для тестирования без Telegram
2. **Для production**: Настройте proper Telegram WebApp authentication
3. **Performance**: Используйте React Query для кеширования API запросов
4. **Security**: Никогда не коммитьте .env файлы с реальными ключами

---

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте логи backend: `uvicorn` output
2. Проверьте браузер console: Network tab
3. Проверьте .env файлы: все ли ключи заполнены
4. API docs: http://localhost:8000/docs

---

Приложение готово к использованию! Все основные функции работают и подключены к real API.

**Удачи с запуском! 🚀**
